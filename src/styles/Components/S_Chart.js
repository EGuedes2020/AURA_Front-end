import styled from "styled-components";

export const ChartStyles = styled.section`
  display: grid;
  gap: var(--chartBar-spacing);
  grid-template-columns: 1fr 1fr 1fr;
  padding: var(--main-spacing);
  height: 15rem;
`;
