import styled from "styled-components";

export const BadgesSectionContainerStyles = styled.section`
  display: grid;
  gap: var(--main-spacing);
  grid-template-columns: 1fr;
  padding: 1rem;
  background-color: #ffffff;
  border-radius: var(--border-card);
`;
