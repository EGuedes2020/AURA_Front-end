import styled from "styled-components";

export const ConquistaEstado = styled.div`
  background-color: var(--color-blue-4);
  border-radius: 1rem;
  padding: 0.5rem 1rem;
  margin-bottom: 0.5rem;

  span {
    font-family: var(--readex-pro);
    font-size: 0.875rem;
    font-weight: var(--medium);
    color: white;
  }
`;
